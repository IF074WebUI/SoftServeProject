import { ConfirmDeleteDirective } from './confirm-delete.directive';

describe('ConfirmDeleteDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmDeleteDirective();
    expect(directive).toBeTruthy();
  });
});
