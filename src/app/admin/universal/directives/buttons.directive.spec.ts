import { ButtonsDirective } from './buttons.directive';

describe('ButtonsDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonsDirective();
    expect(directive).toBeTruthy();
  });
});
