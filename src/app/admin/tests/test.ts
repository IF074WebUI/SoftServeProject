export class Test {
  attempts: number;
  enabled: number;
  enabled_description?: string;
  subject_id: number;
  tasks: number;
  test_id: number;
  test_name: string;
  time_for_test: string;
  subject_name?: string;
}
